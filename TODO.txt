- Treat vault file as key/value store.

- Read password from stdin or environment variable.

- Fail if any unnamed options are provided on the command line.

- Fix the double "ERROR:" prefix when no arguments are specified.
